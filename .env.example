# ============================================================
# Copy this file to `backend/.env` and fill in your real values.
# Never commit the real .env file — only .env.example belongs in git.
# ============================================================

# Port the backend server runs on
PORT=3001

# The URL of your deployed frontend (for CORS). Use * only for local testing.
ALLOWED_ORIGIN=http://localhost:5500

# ---- Supabase ----
# Found in your Supabase project settings → API
SUPABASE_URL=https://vofwsnxrwuouyizquitp.supabase.co
# Use the SERVICE ROLE key (not the anon/public key) — this stays server-side only
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvZndzbnhyd3VvdXlpenF1aXRwIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NzQyNTM3MCwiZXhwIjoyMTAzMDAxMzcwfQ.X1yrMQlRW7McVc4wf83ZH18K8M1FOxm7Ls2mzVc-GOY

# ---- Email notifications (SMTP) ----
# Works with Gmail (use an App Password, not your regular password),
# Outlook, or any SMTP provider (e.g. SendGrid, Mailgun, Resend SMTP).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=punith.n9036@gmail.com
SMTP_PASS=ajuz nldc eoqg wuuc

# Who the notification email is sent to (you) and from (can be same as SMTP_USER)
NOTIFY_EMAIL_TO=punithnnvn@gmail.com
NOTIFY_EMAIL_FROM=punith.n9036@gmail.com
